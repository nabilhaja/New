{
  "name": "bip-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing the BiP Intelligence Platform (Tracker) Open API — notices, awards, and spend data — as tools for LLM clients.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "bip-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
